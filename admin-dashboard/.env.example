# Resend (magic link)
# RESEND_API_KEY=
# RESEND_FROM=   (opzionale; default: Comtra Admin <onboarding@resend.dev>)
# ADMIN_DASHBOARD_URL=

# Unica email ammessa al login magic link (opzionale). Se impostata, il DB non serve per l’auth.
# ALLOWED_ADMIN_EMAIL=admin@comtra.dev

# DB (solo se usi le altre API dashboard: stats, utenti, crediti, ecc.)
# POSTGRES_URL=

# Solo per dev locale
# VITE_ADMIN_API_URL=
# VITE_ADMIN_SECRET=
