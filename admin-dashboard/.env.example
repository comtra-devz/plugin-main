# In produzione (deploy sul progetto dashboard) lascia vuoto: le API sono same-origin (/api/admin).
# In dev locale puoi mettere l'URL della dashboard deployata per chiamare le API in remoto.
# VITE_ADMIN_API_URL=
# Stesso valore di ADMIN_SECRET nel progetto Vercel della dashboard
VITE_ADMIN_SECRET=
